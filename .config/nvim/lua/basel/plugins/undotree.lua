return {
    "mbbill/undotree",
    config = function()
        -- Optional: Custom configuration for the plugin itself
        vim.g.undotree_WindowLayout = 2
        vim.g.undotree_ShortIndicators = 1
    end,
}
