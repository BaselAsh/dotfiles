
# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################
# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=LVDS-1,preferred,0x0,1
monitor=VGA-1,preferred,1366x0,1
# monitor=HDMI-A-1,preferred,1366x-768,1
# 848x480



###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = thunar
$menu = rofi -show drun

$zen-browser = zen-browser
$brave = brave-browser
$qute = qutebrowser


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
# exec-once = swaync
# exec-once = waybar
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP && waybar
exec-once = gsettings set org.gnome.desktop.interface cursor-theme 'capitaine-cursors'
exec-once = gsettings set org.gnome.desktop.interface cursor-size 16
exec-once = gammastep
exec-once = wl-paste --type text --watch bash -c 'cliphist store && cliphist list | tail -n +101 | cliphist delete'
exec-once = wl-paste --type image --watch bash -c 'cliphist store && cliphist list | tail -n +101 | cliphist delete'
exec-once = hyprpaper
exec-once = ~/.local/bin/toggle_mode

# Wallpaper for each workspace
exec-once = /home/baselash/.config/hypr/scripts/wall-sync.sh


# Start Clipboard Manager
# exec-once = wl-paste --type text --watch cliphist store 
# exec-once = wl-paste --type image --watch cliphist store
# Starts the Clipboard Manager with only the last 100 items in it.

# Autostart the music setup on Workspace 10 silently on launch
# Launch ncmpcpp (The main window) on Workspace 10
# exec-once = [workspace 10 silent] kitty -T "ncmpcpp-float" ncmpcpp

# Launch CAVA (The visualizer) on Workspace 10
# It uses the unique title we set earlier so the window rules above apply.
# exec-once = [workspace 10 silent] kitty -T "cava-visualizer" cava

# Ensure ncmpcpp is on top of CAVA
# exec-once = sleep 0.1 && hyprctl dispatch focuswindow title:^ncmpcpp-float$

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

# env = XCURSOR_THEME, breeze_cursor
# env = XCURSOR_SIZE,16
# env = HYPRCURSOR_THEME, breeze_cursor
# env = HYPRCURSOR_SIZE,16
# Capitaine Cursors
# Capitaine Cursors
env = XCURSOR_THEME, capitaine-cursors
env = XCURSOR_SIZE,16
env = HYPRCURSOR_THEME, capitaine-cursors
env = HYPRCURSOR_SIZE,16


# Making sure to use the right desktop
env = XDG_CURRENT_DESKTOP, Hyprland


###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

permission = /usr/(bin|local/bin)/grim, screencopy, allow
permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 2
    gaps_out = 3

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(89b4faFF)
    col.inactive_border = rgba(BB5100FF)
    # col.inactive_border = rgba(1e1e2eee)
    # Old Configuration
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 0
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        # false because of GPU load
        enabled = false 
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        # false because of GPU load
        enabled = false
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}


# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = false, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
    vfr = true
}


#############
### INPUT ###
#############


# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us, ara
    kb_variant =
    kb_model =
    kb_options = grp:alt_shift_toggle
    kb_rules =

    follow_mouse = 1
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = instant-usb-optical-mouse
    sensitivity = -0.7
}
device {
    name = compx-2.4g-receiver-1
    sensitivity = -0.5
}
device{
    name = synps/2-synaptics-touchpad
    sensitivity = 0.2
}
# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    # workspace_swipe = true
}

# Rule to hide MPV windows when playing audio (no video stream)
# windowrule = float, class:^(mpv)$ # Ensure mpv is floatable
# windowrule = size 1 1, class:^(mpv)$  # Shrink it to 1x1 pixel
# windowrule = opacity 0.0 override, class:^(mpv)$ # Make it invisible
# windowrule = move 10000 10000, class:^(mpv)$ # Move it far off-screen

###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

#This is my own configurations
#==========================================================================
# Connecting and Disconnecting the earbuds
bind = SUPER, F2, exec, /home/baselash/.config/commands/cut-ears.py
bind = SUPER, F3, exec, /home/baselash/.config/commands/ears.py





# Screenshot active window to Pictures folder
bind = $mainMod, s, exec, ~/.local/bin/hypr-screenshot active

# Screenshot selected area to Pictures folder
bind = $mainMod SHIFT, s, exec, ~/.local/bin/hypr-screenshot area


# Toggle Waybar
bind = $mainMod, B, exec, killall -SIGUSR1 waybar

bind = $mainMod Shift, B, exec, killall waybar && waybar

# Toggle Night Light (Gammastep)
bind = $mainMod SHIFT, N, exec, pkill gammastep || gammastep

# Clipboard with Image Previews
bind = $mainMod, semicolon, exec, cliphist list | ~/.local/bin/cliphist-rofi-img | rofi -dmenu -p "Clipboard" -show-icons -theme ~/.config/rofi/clipboard.rasi | cliphist decode | wl-copy

# Function keys binds
bind = ,  XF86Calculator, exec, qalculate-gtk
bind = ,  XF86HomePage, exec, $zen-browser

# Open system monitor
bind = $mainMod, ESCAPE, exec, kitty btop 

# Suspend
bind = $mainMod, DELETE, exec, sleep 1 && systemctl suspend

# Windows/Workspaces preview
bind = $mainMod, TAB, exec, rofi -show window

# Lock the Screen
bind = $mainMod, BACKSLASH, exec, hyprlock

bind = $mainMod, GRAVE, exec, wlogout

bind = $mainMod, T, exec, [size 60% 70%; float; center;] kitty -e nvim /home/baselash/Journal/todo.md
bind = $mainMod Shift, P, exec, [size 60% 70%; float; center;] kitty -e nvim /home/baselash/Journal/projects.md
bind = $mainMod, N, exec, [size 60% 70%; float; center;] kitty -e nvim 



# MPD
# Media Keys for MPD control (using mpc)
bind = , XF86AudioPlay, exec, mpc toggle
bind = , XF86AudioStop, exec, mpc stop
bind = , XF86AudioNext, exec, mpc next
bind = , XF86AudioPrev, exec, mpc prev

# Optional: Open ncmpcpp in a new kitty window (assuming it's your preferred terminal)
bind = , XF86Tools, exec, [size 60% 70%; float; center;] kitty -T "ncmpcpp-float" ncmpcpp
bind = , XF86Tools, exec, kitty -T "cava-visualizer" cava

# Mail
bind = , XF86Mail, exec, $zen-browser mail.google.com

# --- CAVA (Visualizer) Rules ---
windowrulev2 = workspace 10, title:^(cava-visualizer)$
windowrulev2 = noborder, title:^(cava-visualizer)$    # Remove the border
# windowrulev2 = nofocus, title:^(cava-visualizer)$     # Don't steal keyboard focus
windowrulev2 = dimaround, title:^(cava-visualizer)$

# --- ncmpcpp (Client) Rules ---
# We will use a unique title for the ncmpcpp window as well for precision.
# Change the workspace rule to target the specific title.
windowrulev2 = workspace 10, title:^(ncmpcpp-float)$
windowrulev2 = float, title:^(ncmpcpp-float)$        # Make ncmpcpp float over CAVA
windowrulev2 = center, title:^(ncmpcpp-float)$       # Center it on the screen
windowrulev2 = size 50% 50%, title:^(ncmpcpp-float)$ # Adjust size as needed (e.g., 50% of screen)


# Open Notifications
# bind = $mainMod, G, exec, swaync-client -t

# Open VSCode
bind = $mainMod Shift, return, exec, code

# Hyprpaper reload
bind = $mainMod Shift, F5, exec, killall hyprpaper && hyprpaper
bind = $mainMod Shift, B, exec, killall waybar && waybar 

bind = $mainMod, period, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Performace Mode
bind = $mainMod Shift, escape, exec, ~/.local/bin/toggle_mode


# 1: Terminal (Main)
bind = SUPER CTRL, 1, workspace, 1
bind = SUPER CTRL, 1, exec, pgrep -x kitty || kitty

# 2: Neovim (Coding)
bind = SUPER CTRL, 2, workspace, 2
bind = SUPER CTRL, 2, exec, pgrep -x nvim || kitty -e nvim

# 3: Browser (Research/Docs)
bind = SUPER CTRL, 3, workspace, 3
bind = SUPER CTRL, 3, exec, pgrep -x $zen-browser || $zen-browser

# 4: React/Frontend Dev
bind = SUPER CTRL, 4, workspace, 4
bind = SUPER CTRL, 4, exec, pgrep -x "npm" || foot

# 5: PostgreSQL/Database
bind = SUPER CTRL, 5, workspace, 5
bind = SUPER CTRL, 5, exec, pgrep -x psql || kitty -e psql

# 6: Files
bind = SUPER CTRL, 6, workspace, 6
bind = SUPER CTRL, 6, exec, $fileManager

# 7: Study/Documentation
bind = SUPER CTRL, 7, workspace, 7
bind = SUPER CTRL, 7, exec, pgrep -x qpdfview || qpdfview

# 8: Discord (Communication)
bind = SUPER CTRL, 8, workspace, 8
bind = SUPER CTRL, 8, exec, pgrep -x vesktop || vesktop


# 9: System Monitor (btop)
bind = SUPER CTRL, 9, workspace, 9
bind = SUPER CTRL, 9, exec, pgrep -x btop || kitty -e btop

# 10: Music
bind = SUPER CTRL, 0, workspace, 10
bind = SUPER CTRL, 0, exec, [size 60% 70%; float; center;] kitty -T "ncmpcpp-float" ncmpcpp
bind = SUPER CTRL, 0, exec, pgrep -x cava || kitty -T "cava-visualizer" cava

# Odd numbered workspaces on main monitor
workspace = 1, monitor:LVDS-1, default:true
workspace = 3, monitor:LVDS-1
workspace = 5, monitor:LVDS-1
workspace = 7, monitor:LVDS-1
workspace = 9, monitor:LVDS-1

# Even numbered workspaces on secondary monitor
workspace = 2, monitor:VGA-1, default:true
workspace = 4, monitor:VGA-1
workspace = 6, monitor:VGA-1
workspace = 8, monitor:VGA-1
workspace = 10, monitor:VGA-1

# --- Workspace & Wallpaper Binds ---

# LVDS-1 Monitor (Workspaces 1, 3, 5, 7, 9)
# bind = $mainMod, 1, exec, hyprctl hyprpaper wallpaper "LVDS-1,/home/baselash/.config/hypr/Wallpapers/Arch-Wallpaper.jpg"
# bind = $mainMod CTRL, 1, exec, hyprctl hyprpaper wallpaper "LVDS-1,/home/baselash/.config/hypr/Wallpapers/Arch-Wallpaper.jpg"
# bind = $mainMod, 2, exec, hyprctl hyprpaper wallpaper "VGA-1,/home/baselash/.config/hypr/Wallpapers/Coding-Wallpaper.jpg"
# bind = $mainMod CTRL, 2, exec, hyprctl hyprpaper wallpaper "VGA-1,/home/baselash/.config/hypr/Wallpapers/Coding-Wallpaper.jpg"
# bind = $mainMod, 3, exec, hyprctl hyprpaper wallpaper "LVDS-1,/home/baselash/.config/hypr/Wallpapers/Web-Wallpaper.jpg"
# bind = $mainMod CTRL, 3, exec, hyprctl hyprpaper wallpaper "LVDS-1,/home/baselash/.config/hypr/Wallpapers/Web-Wallpaper.jpg"
# bind = $mainMod, 4, exec, hyprctl hyprpaper wallpaper "VGA-1,/home/baselash/.config/hypr/Wallpapers/Server-Wallpaper.jpg"
# bind = $mainMod CTRL, 4, exec, hyprctl hyprpaper wallpaper "VGA-1,/home/baselash/.config/hypr/Wallpapers/Server-Wallpaper.jpg"
# bind = $mainMod, 5, exec, hyprctl hyprpaper wallpaper "LVDS-1,/home/baselash/.config/hypr/Wallpapers/Database-Wallpaper.jpg"
# bind = $mainMod CTRL, 5, exec, hyprctl hyprpaper wallpaper "LVDS-1,/home/baselash/.config/hypr/Wallpapers/Database-Wallpaper.jpg"
# bind = $mainMod, 6, exec, hyprctl hyprpaper wallpaper "VGA-1,/home/baselash/.config/hypr/Wallpapers/Study-Wallpaper.jpg"
# bind = $mainMod CTRL, 6, exec, hyprctl hyprpaper wallpaper "VGA-1,/home/baselash/.config/hypr/Wallpapers/Study-Wallpaper.jpg"
# bind = $mainMod, 7, exec, hyprctl hyprpaper wallpaper "LVDS-1,/home/baselash/.config/hypr/Wallpapers/Work-Wallpaper.jpg"
# bind = $mainMod CTRL, 7, exec, hyprctl hyprpaper wallpaper "LVDS-1,/home/baselash/.config/hypr/Wallpapers/Work-Wallpaper.jpg"
# bind = $mainMod, 8, exec, hyprctl hyprpaper wallpaper "VGA-1,/home/baselash/.config/hypr/Wallpapers/Social-Wallpaper.jpg"
# bind = $mainMod CTRL, 8, exec, hyprctl hyprpaper wallpaper "VGA-1,/home/baselash/.config/hypr/Wallpapers/Social-Wallpaper.jpg"
# bind = $mainMod, 9, exec, hyprctl hyprpaper wallpaper "LVDS-1,/home/baselash/.config/hypr/Wallpapers/Computer-Hardware-Wallpaper.jpg"
# bind = $mainMod CTRL, 9, exec, hyprctl hyprpaper wallpaper "LVDS-1,/home/baselash/.config/hypr/Wallpapers/Computer-Hardware-Wallpaper.jpg"
# bind = $mainMod, 0, exec, hyprctl hyprpaper wallpaper "VGA-1,/home/baselash/.config/hypr/Wallpapers/Audio-Wallpaper.png"
# bind = $mainMod CTRL, 0, exec, hyprctl hyprpaper wallpaper "VGA-1,/home/baselash/.config/hypr/Wallpapers/Audio-Wallpaper.png"


#==========================================================================



# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, return, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, space, exec, $menu
bind = $mainMod, W, exec, $zen-browser
bind = $mainMod SHIFT, W, exec, $qute 
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, slash, togglesplit, # dwindle
bind = $mainMod SHIFT, R, exec, hyprctl reload


# Move focus with mainMod + arrow keys

bind = $mainMod, left, movefocus, l
bind = $mainMod, down, movefocus, d
bind = $mainMod, up, movefocus, u
bind = $mainMod, right, movefocus, r

bind = $mainMod, h, movefocus, l
bind = $mainMod, j, movefocus, d
bind = $mainMod, k, movefocus, u
bind = $mainMod, l, movefocus, r

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Move active window to a workspace mainMod + Shift (Arrow keys or vim bindings)
bind = $mainMod SHIFT, right, movetoworkspace, 2
bind = $mainMod SHIFT, left, movetoworkspace, 1
bind = $mainMod SHIFT, up, movetoworkspace, e+1
bind = $mainMod SHIFT, down, movetoworkspace, e-1

bind = $mainMod SHIFT, l, movetoworkspace, 2
bind = $mainMod SHIFT, h, movetoworkspace, 1
bind = $mainMod SHIFT, k, movetoworkspace, e+1
bind = $mainMod SHIFT, j, movetoworkspace, e-1


# Example special workspace (scratchpad)
bind = $mainMod, m, togglespecialworkspace, magic
bind = $mainMod SHIFT, m, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bindel = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bindel = ,XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bindel = ,XF86AudioMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$
#
# Ignore maximize requests from apps. You'll probably like this.
# windowrule = suppressevent maximize, class:.*
#
# Fix some dragging issues with XWayland
# windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
